# Структура локальной базы данных

## Обзор

Voidlace использует **IndexedDB** через библиотеку **Dexie** для хранения всех данных локально в браузере пользователя.

## Таблицы

### 1. novels (Произведения)
```typescript
{
  id: string;              // Уникальный ID
  title: string;           // Название
  author: string;          // Автор
  cover: string;           // URL обложки
  rating: number;          // Рейтинг (0-5)
  ratingCount: number;     // Количество оценок
  description: string;     // Описание
  genres: string[];        // Жанры
  tags: string[];          // Теги
  status: string;          // ongoing | completed | hiatus
  type: string;            // ranobe | webnovel | fanfic | original
  year: number;            // Год выпуска
  views: number;           // Просмотры
  favorites: number;       // В избранном
  volumes: Volume[];       // Тома и главы
  totalChapters: number;   // Всего глав
}
```

### 2. users (Пользователи)
```typescript
{
  id: string;              // Уникальный ID
  username: string;        // Имя пользователя
  email: string;           // Email
  role: string;            // user | editor | admin
  avatar: string;          // URL аватара
  createdAt: string;       // Дата создания
  bio?: string;            // Биография
  avatarFrame?: {          // Рамка аватара
    enabled: boolean;
    color: string;
    animation: string;
    thickness: number;
  };
}
```

### 3. library (Библиотека пользователя)
```typescript
{
  userId: string;          // ID пользователя
  novelId: string;         // ID произведения
  addedDate: string;       // Дата добавления
  isFavorite: boolean;     // В избранном
  status: string;          // reading | completed | plan-to-read | dropped
}
```

### 4. progress (Прогресс чтения)
```typescript
{
  userId: string;          // ID пользователя
  novelId: string;         // ID произведения
  chapterId: string;       // ID главы
  progress: number;        // Прогресс (0-100%)
  lastRead: string;        // Дата последнего чтения
  scrollPosition: number;  // Позиция скролла
}
```

### 5. comments (Комментарии)
```typescript
{
  id: string;              // Уникальный ID
  novelId: string;         // ID произведения
  userId: string;          // ID пользователя
  userName: string;        // Имя пользователя
  userAvatar?: string;     // Аватар пользователя
  content: string;         // Текст комментария
  rating?: number;         // Оценка (опционально)
  likes: number;           // Количество лайков
  likedBy: string[];       // ID пользователей, поставивших лайк
  createdAt: string;       // Дата создания
  updatedAt: string;       // Дата обновления
}
```

## API для работы с БД

### novelDB
```typescript
novelDB.getAll()                    // Получить все произведения
novelDB.getById(id)                 // Получить по ID
novelDB.insert(novel)               // Добавить произведение
novelDB.update(novel)               // Обновить произведение
novelDB.delete(id)                  // Удалить произведение
novelDB.search(query)               // Поиск по названию/автору
novelDB.bulkInsert(novels)          // Массовое добавление
```

### userDB
```typescript
userDB.getAll()                     // Получить всех пользователей
userDB.getById(id)                  // Получить по ID
userDB.getByUsername(username)      // Получить по имени
userDB.insert(user)                 // Добавить пользователя
userDB.update(user)                 // Обновить пользователя
userDB.delete(id)                   // Удалить пользователя
```

### libraryDB
```typescript
libraryDB.getAll(userId)            // Получить библиотеку пользователя
libraryDB.add(item)                 // Добавить в библиотеку
libraryDB.remove(userId, novelId)   // Удалить из библиотеки
libraryDB.toggleFavorite(userId, novelId) // Переключить избранное
```

### progressDB
```typescript
progressDB.getAll(userId)           // Получить весь прогресс
progressDB.update(progress)         // Обновить прогресс
```

### commentDB
```typescript
commentDB.getAll()                  // Получить все комментарии
commentDB.getByNovelId(novelId)     // Комментарии к произведению
commentDB.getByUserId(userId)       // Комментарии пользователя
commentDB.insert(comment)           // Добавить комментарий
commentDB.update(comment)           // Обновить комментарий
commentDB.delete(id)                // Удалить комментарий
commentDB.toggleLike(commentId, userId) // Лайк/дизлайк
```

## Экспорт/Импорт

### Экспорт всей БД
```typescript
import { exportDatabase } from './db/database';

const jsonData = await exportDatabase();
// Возвращает JSON со всеми данными
```

### Импорт БД
```typescript
import { importDatabase } from './db/database';

await importDatabase(jsonData);
// Восстанавливает всю БД из JSON
```

### Статистика БД
```typescript
import { getDatabaseStats } from './db/database';

const stats = await getDatabaseStats();
// { novels: 100, users: 5, library: 50, progress: 30, comments: 200 }
```

## Синхронизация

### Автоматическая синхронизация
- ✅ Сохранение в облако каждые 5 минут
- ✅ Сохранение при закрытии страницы
- ✅ Сохранение при изменении данных

### Ручная синхронизация
- **Сохранить в облако** - экспортирует всю БД в Upstash
- **Загрузить из облака** - импортирует БД с другого устройства

### Что синхронизируется
- ✅ Все произведения
- ✅ Все пользователи
- ✅ Библиотека
- ✅ Прогресс чтения
- ✅ Комментарии
- ✅ Настройки

## Преимущества локальной БД

1. **Быстрая работа** - все данные в браузере
2. **Оффлайн режим** - работает без интернета
3. **Приватность** - данные только у вас
4. **Гибкость** - легко добавлять новые функции
5. **Синхронизация** - экспорт/импорт между устройствами

## Лимиты

- **IndexedDB**: обычно 50% от свободного места на диске
- **Upstash Redis**: 256 MB на бесплатном плане
- **Рекомендация**: до 1000 произведений, 10000 комментариев

## Миграции

При обновлении структуры БД используется система версий Dexie:

```typescript
this.version(4).stores({
  // Новая структура
}).upgrade(async (trans) => {
  // Код миграции данных
});
```

Данные автоматически мигрируют при обновлении версии.

## Безопасность

- ✅ Данные хранятся локально в браузере
- ✅ Нет доступа с других устройств без синхронизации
- ✅ Можно очистить через настройки браузера
- ✅ Синхронизация через защищённый API (Upstash)

## Резервное копирование

### Автоматическое
- Экспорт в облако каждые 5 минут (если настроен Upstash)

### Ручное
1. Профиль → "Экспорт в файл"
2. Сохраняется JSON файл со всей БД
3. Импорт через "Импорт из файла"

Готово! 🚀
