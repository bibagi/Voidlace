# 🚀 Деплой на Vercel с синхронизацией

## Шаг 1: Подготовка

У вас уже есть:
- ✅ Upstash Redis база данных
- ✅ Credentials (URL и TOKEN)
- ✅ Настроенный проект

## Шаг 2: Деплой на Vercel

### Вариант А: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub
2. Загрузите туда проект:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/voidlace.git
git push -u origin main
```

3. Перейдите на https://vercel.com
4. Нажмите **"Add New Project"**
5. Выберите ваш репозиторий
6. Нажмите **"Import"**

### Вариант Б: Через Vercel CLI

```bash
npm install -g vercel
vercel login
vercel
```

## Шаг 3: Настройка переменных окружения

1. В Vercel откройте ваш проект
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте две переменные:

```
Name: UPSTASH_REDIS_REST_URL
Value: https://magical-wallaby-22552.upstash.io
Environments: ✅ Production ✅ Preview ✅ Development
```

```
Name: UPSTASH_REDIS_REST_TOKEN
Value: AVgYAAIncDI1MWQyMzk1ZTgxOTM0YWYzOWQ4NDAyNTM4ZWY5YTQ2ZHAyMjI1NTI
Environments: ✅ Production ✅ Preview ✅ Development
```

4. Нажмите **"Save"**

## Шаг 4: Redeploy

1. Перейдите в **Deployments**
2. Нажмите на последний деплой
3. Нажмите **⋯** → **Redeploy**
4. Подождите завершения

## Шаг 5: Проверка

1. Откройте ваш сайт (например: `https://voidlace.vercel.app`)
2. Войдите в аккаунт
3. Перейдите в профиль
4. Нажмите **"Сохранить в облако"**
5. Должно появиться: `✅ Данные сохранены в облако`

## Проверка API

Откройте в браузере:
```
https://ваш-сайт.vercel.app/api/sync
```

Должна быть ошибка 400 (это нормально, значит API работает!)

## Troubleshooting

### Ошибка "Redis not configured"
- Проверьте, что переменные добавлены в Vercel
- Убедитесь, что нет опечаток
- Сделайте Redeploy

### Ошибка 401 Unauthorized
- Проверьте правильность токена
- Убедитесь, что используете REST API credentials

### API не работает
- Проверьте, что файл `api/sync.ts` существует
- Проверьте логи в Vercel Dashboard → Functions

## Локальная разработка с Vercel

Для тестирования API локально:

```bash
npm install -g vercel
vercel dev
```

Это запустит локальный сервер с поддержкой Vercel Functions.

---

**Готово! Теперь у вас полноценная облачная синхронизация! ☁️✨**
