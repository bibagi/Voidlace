# Деплой на Vercel

## ✅ Исправлены все ошибки сборки

1. ✅ Добавлены типы для Vite (`src/vite-env.d.ts`)
2. ✅ Исправлены async функции в Login и Register
3. ✅ Добавлен `.env` в `.gitignore`

## 🚀 Деплой БЕЗ Firebase (работает сразу)

Если вы **не настроили Firebase**, приложение всё равно будет работать:
- ✅ Все функции работают
- ✅ Данные сохраняются локально
- ✅ Используйте экспорт/импорт файлов для синхронизации

Просто задеплойте на Vercel - всё заработает!

## 🔥 Деплой С Firebase (облачная синхронизация)

### Шаг 1: Настройте Firebase локально

1. Создайте проект Firebase (см. `FIREBASE_SETUP.md`)
2. Создайте файл `.env` с конфигурацией
3. Проверьте локально: `npm run dev`

### Шаг 2: Добавьте переменные в Vercel

1. Откройте ваш проект на Vercel
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте каждую переменную:

```
VITE_FIREBASE_API_KEY = ваш_ключ
VITE_FIREBASE_AUTH_DOMAIN = ваш_домен
VITE_FIREBASE_PROJECT_ID = ваш_проект_id
VITE_FIREBASE_STORAGE_BUCKET = ваш_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID = ваш_sender_id
VITE_FIREBASE_APP_ID = ваш_app_id
```

4. Выберите **Production**, **Preview**, и **Development**
5. Нажмите **Save**

### Шаг 3: Redeploy

1. Перейдите в **Deployments**
2. Нажмите на последний деплой
3. Нажмите **⋯** → **Redeploy**
4. Готово! 🎉

## 📝 Проверка

После деплоя:

1. Откройте сайт
2. Откройте консоль браузера (F12)
3. Должно быть:
   - **С Firebase**: `✅ Firebase инициализирован`
   - **Без Firebase**: `⚠️ Firebase не настроен`

Оба варианта работают корректно!

## 🔧 Troubleshooting

### Ошибка сборки на Vercel

Если видите ошибки TypeScript:
1. Убедитесь что закоммитили `src/vite-env.d.ts`
2. Проверьте что `npm run build` работает локально

### Firebase не работает на Vercel

1. Проверьте что все переменные добавлены
2. Проверьте что нет опечаток в названиях
3. Сделайте Redeploy после добавления переменных

### Сайт работает, но нет синхронизации

Это нормально если Firebase не настроен!
Используйте кнопки "Экспорт/Импорт файла" в профиле.

## 🎯 Итог

- **Без Firebase**: Работает локальное хранение + экспорт/импорт
- **С Firebase**: Работает облачная синхронизация между устройствами

Выбирайте что вам нужно! 🚀
