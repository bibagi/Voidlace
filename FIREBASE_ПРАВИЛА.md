# 🔒 Настройка правил безопасности Firebase

## Шаг 1: Откройте Firebase Console

1. Перейдите на https://console.firebase.google.com/
2. Выберите проект **voidlace-77e77**
3. В левом меню выберите **Realtime Database**
4. Перейдите на вкладку **Правила** (Rules)

## Шаг 2: Замените правила

Удалите текущие правила и вставьте эти:

```json
{
  "rules": {
    "users": {
      "$userId": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```

## Шаг 3: Опубликуйте

Нажмите кнопку **"Опубликовать"** (Publish)

## Что это дает

- ✅ Все пользователи могут читать данные других (для онлайн статуса)
- ✅ Каждый может записывать свои данные
- ✅ Работает без авторизации Firebase Auth

## Безопасность

Для продакшна можно улучшить правила:

```json
{
  "rules": {
    "users": {
      "$userId": {
        "status": {
          ".read": true,
          ".write": true
        },
        "info": {
          ".read": true,
          ".write": true
        },
        "data": {
          ".read": "$userId === auth.uid || auth == null",
          ".write": "$userId === auth.uid || auth == null"
        }
      }
    }
  }
}
```

Но для начала используйте простые правила выше.

---

**После настройки правил синхронизация заработает! 🚀**
